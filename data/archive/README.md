# Archived data

`rmp_subset_synthetic.csv` was a small **synthetic** demo file (repeated templates for MIT/Stanford/etc.). It is **not** used anymore.

The app now imports real data from public sources — see [`../DATA_SOURCES.md`](../DATA_SOURCES.md).
